"""The cart app has no models — the cart lives in the session.

See ``cart/cart.py`` for the Cart class. This module is kept (empty) so the app
layout stays conventional and so a future DB-backed cart has an obvious home.
"""
